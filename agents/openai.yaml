interface:
  display_name: "Promptsmith"
  short_description: "Craft or refine prompts using best practices"
  default_prompt: "Use $promptsmith to craft a prompt for the task I describe."
policy:
  allow_implicit_invocation: false
